import { defineStore } from 'pinia';
import { SetupStoreId } from '@/enum';

export const useCocoConfigStore = defineStore(SetupStoreId.CocoConfig, () => {
  // const cocoConfig: CocoApi.CocoConfig.CocoConfigList = reactive(fetchCocoConfig());
  // return {
  //   cocoConfig
  // };
});
