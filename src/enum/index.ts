export enum SetupStoreId {
  App = 'app-store',
  Theme = 'theme-store',
  Auth = 'auth-store',
  Route = 'route-store',
  Tab = 'tab-store',
  CocoConfig = 'coco-config'
}

export enum SsoAuthor {
  Authing = 'authing'
}
